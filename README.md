# TODO
1. Add line numbers to editor
2. Make an OpenSSL SecureStream class that is a ostream and istream. It is constructed on a stream object and encrypts/decrypts data automatically.
3. Add secure-allocator
4. Make buffer allocator aware
5. Make buffer use securely allocated strings
6. Add encryption-file method
7. Fix the hard coded MonoLisaRegular font