# TODO
1. Make an OpenSSL SecureStream class that is a ostream and istream. It is constructed on a stream object and encrypts/decrypts data automatically.
2. Add secure-allocator
3. Make buffer allocator aware
4. Make buffer use securely allocated strings
5. Add encryption-file method